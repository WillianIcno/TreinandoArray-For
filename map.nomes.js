// padroniza os nomes tbm, gap e mais utilizado 

const nomes = ['ana Julia', 'Caio vinicius', 'BIA silva']

const nomesPadronizados = nomes.map((nome) =>  nome.toUpperCase(nomes))
console.log(nomesPadronizados)