// a função .concat serve para pegar duas array distintas e transformar elas em uma única array 
// voce precisa criar uma nova constante  para criar uma nova array, logo em seguida faça a concatenação de ambas usando .concat

const salaJs = ["Evaldo", "Camis", "Mari"];
const salaPython = ["Ju", "Leo", "Raquel"];

const salasJuntas = salaPython.concat(salaJs);

console.log(salasJuntas)

// a ordem das constantes vai interfirir diretamente no padrao da nova array que será criada 